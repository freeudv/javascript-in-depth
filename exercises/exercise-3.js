  //Создайте функцию `print`, с локальной переменной `name` и локальной функцией `log`, в которой имеется локальная переменная `output`:

function print() {
  var name;

  function log() {
    var output;
  }
}